#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {

    printf ( "deberes:\n" );
    printf ( "tenemos tres variables, r,g,b, le preguntamos que color ve en las 3 variables(muchos if)" )


    return EXIT_SUCCESS;
}
