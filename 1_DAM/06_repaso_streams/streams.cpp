
#include <stdio.h>
#include <stdlib.h>

#define hola 50 /* define una variable*/

int main (int argc, char *argv[]) {
    for(int i=0;i<=hola;i++) /*hace un bucle para que imprima las que veces que este definida la variable */
    printf("*");

    printf("\n");
    return EXIT_SUCCESS;
}
